# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
python-dotenv==1.0.0

# System monitoring & Docker
docker==7.0.0
psutil==5.9.6
requests==2.31.0
aiofiles==23.2.1

# API & validation
pydantic==2.5.0
pydantic-settings==2.1.0
httpx==0.25.2

# Discord integration (optional)
py-cord==2.4.1

# AI integration (OPTIONAL - disable if hardware constraints)
# ollama==0.0.48  # Uncomment for AI features (requires 8GB+ VRAM)
